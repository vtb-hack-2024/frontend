export default function AuthLayout({children}) {
    return (
        // <div className="wrapper flex justify-center items-center h-screen w-full">
        <div className="wrapper pt-[200px] w-full">
            {children}
        </div>
    )
}